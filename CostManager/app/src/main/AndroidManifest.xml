<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.costmanager">

    <application
        android:allowBackup="true"
        android:icon="@drawable/_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditMonth"></activity>
        <activity android:name=".NeedHelp" />
        <activity android:name=".TipsActivity2" />
        <activity android:name=".SaveTipsActivity" />
        <activity android:name=".NoteActivity" />
        <activity android:name=".Statistics" />
        <activity android:name=".CalculatorActivity" />
        <activity android:name=".IncomeActivity" />
        <activity android:name=".Yearly" />
        <activity android:name=".EditActivity" />
        <activity android:name=".Monthly" />
        <activity android:name=".ActivityTwo" />
        <activity android:name=".dailyone" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>